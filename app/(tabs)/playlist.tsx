import { GlobalStyles as gs } from '@/styles/styles';
import { Text, View } from 'react-native';

export default function PlaylistScreen() {
    return (
        <View style={gs.screen}>
            <Text style={gs.h2}>Playlist</Text>
        </View>
    );
}